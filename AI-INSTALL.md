# 给 AI 的安装说明书

> **你现在读到的这份文件是写给 AI 的，不是写给人看的。**
> 你的任务是：判断该不该装、**先问用户**、然后按步骤装、最后验证。
> 如果你不是 AI，请读 [`README.md`](README.md)。

---

## 0. 先做一件事：**不要直接装**

这个插件会**改你的配置文件、创建数据库、并在后续每一轮对话里注入内容**。
这属于**不可逆动作**。所以：

> **在动手之前，你必须先向用户说明，并拿到他的同意。**

**照抄下面这段话去问用户**（按你的实际情况替换尖括号里的内容）：

```
我看了这个叫 plan-anchor（计划锚）的东西，它想解决的是"计划做着做着就被新问题带跑、
原计划全忘了"这个毛病。做法是：把你的计划落盘成一个锚，每轮对话都把它顶到我眼前，
中途冒出来的问题必须先显式处置（现在就做 / 记下来回头做 / 判定不做），而不是直接追下去。

装它要动这些东西：<列出第 4 节里与你相关的项>。
它治不了的东西我也说清楚：<列出第 2 节>。

好处：计划不容易被忘、冒出来的问题有处放、你要问"我们做到哪了"我能照实答。
代价：会多几次记录动作；它会在我跑偏时插话提醒；它只在我主动申报时有效，
我不申报直接干活的话它看不见。

要装吗？
```

---

## 1. 这是什么

一个**防跑偏的护栏**，安装在你和宿主之间。

它把「计划」从**上下文里的临时文字**变成**磁盘上的持久锚**：

- 计划与步骤**落盘**，跨会话、跨上下文压缩存活
- 每轮对话把锚**顶回 AI 眼前**（这是它最承重的部分）
- 中途发现的新问题**必须显式处置**：`permit`（现在就做）/ `defer`（记下来，带回程条件）/ `decline`（判定不做）
- 被判"暂不做"的事进**泊位**，并且带**回程票**——到条件了会主动提醒，不靠 AI 记
- 连续多轮没推进计划 → 提醒；而 AI 若确实在做当前步的长活，可以用 `plan_note` 声明"我在轨"并把预算清零

## 2. 它治什么病

用户的原话（这是这个项目的起点）：

> "执行其中一规划的时候又发现了很多其他的问题，然后就**一路沿着其他的问题进行下去了**，
> 导致**其他的规划全忘记了，并且混乱了**"

拆开是四步：`① 发现新问题 → ② 一路追下去 → ③ 原计划全忘了 → ④ 混乱`

**这个插件打的是 ③，并给 ② 提供了一个更好的选项。它不打 ① 和 ②** —— 因为"发现问题就去处理"本身不是罪，有时候那个问题就是阻塞。

## 3. ⚠️ 它**不**治什么（这一节最重要，请照实转告用户）

1. **它只在你主动申报时有效。** 它看得见的是**工具调用**；如果你不调任何它的工具就埋头干活，它一声不响。极端情况下它完全看不见你。
2. **它不能阻止 AI 跑偏，只能让跑偏"不被忘记、不被隐藏"。** 它的立场是：**偏离可以，沉默不行。**
3. **它防不住改数据库。** 能改你磁盘上文件的程序，你没法用同一个磁盘上的东西去约束它。它能抓到的只是窄范围的篡改。
4. **它不判断"做得对不对"。** 验收标准是语义判断，它只检查组内可观测的事实（依据是否为空、是否回应了你写的验收措辞），**判断始终留给人**。

> 如果你把这四条读给用户听而他仍然想装，那才是知情的同意。

## 4. 装了会改动什么（逐项，请照实转告）

| 会动什么 | 具体 |
|---|---|
| **宿主的插件清单** | 在 profile / 配置里登记这个插件（DSH：`package.json` 的 `dependencies` + `dsh.profile.bundles`） |
| **创建一个数据库文件** | `$DSH_HOME/plan-anchor/plan.db`（SQLite）。**不含任何用户代码，只存计划与台账** |
| **运行时的行为** | 后续每轮对话可能注入提醒（每回合最多一次锚；信号命中时一次；漂移提醒每步每档一次） |
| **不会做的事** | 不改你的代码文件 · 不联网 · 不上传任何东西 · 不使用你的凭据 |

## 5. 装（以 DSH 为参考实现）

### 环境要求（**这一条请先核对，否则一装就崩**）

| 依赖 | 要求 | 证据强度 |
|---|---|---|
| **Node.js** | **≥ 22.13.0**（或 ≥ 23.4.0） | **官方文档** [nodejs.org/api/sqlite.md](https://nodejs.org/api/sqlite.md)：`node:sqlite` *added in v22.5.0*，但当时**在 `--experimental-sqlite` 标志之后**；该标志在 **v23.4.0 / v22.13.0** 被移除 |
| | v24.19.0 上**跑通** | **本机实测**（`DatabaseSync` 建表、写入、查询全通过） |
| | 22.13 / 23.4 **未实测** | ⚠️ 尝试用 `npx node@<版本>` 验证时**拉取超时**，未能得到可靠结论 |

> **诚实注记**：上表第一行是**引用官方文档**，不是我们实测的结果；第二行是实测。
> 如果你的环境是 22.5 ~ 22.12，`node:sqlite` 需要加 `--experimental-sqlite` 标志才能用。
> 我们用到的 API 只有 `DatabaseSync` / `exec` / `prepare` / `run` / `get` / `all`，都是 v22.5.0 就有的；
> **真正卡版本的是"要不要加标志"这件事**。

```bash
# 1) 把本仓库放到一个固定位置（用绝对路径，不要用临时目录）
# 2) 装进目标 profile
dsh plugin --profile <profile-name> add file:<本仓库的绝对路径>

# 3) 确认登记成功 —— 看 profile 的 package.json（这条是实测过的路径）
#    应该能看到 "dsh-plan-anchor": "file:<本仓库绝对路径>"
#    且 dsh.profile.bundles 里包含 "dsh-plan-anchor"
cat $DSH_HOME/profiles/<profile-name>/package.json

# 4) 重启宿主（插件在启动时加载，装完必须重启）
```

> **注**：`dsh plugin` 是 pnpm 的包装（`dsh plugin --help` 输出的是 pnpm 的帮助）。
> 本文档不写未经验证的命令 —— `--dump-config` 之类**不要照抄**，它们在当前版本不存在。

> **注意**：`file:` 依赖是**拷贝**而不是软链。改了源码要重新 `add` 或手动同步，否则跑的还是旧代码。

### 其他宿主

参考实现是 DSH 专用的（用了宿主的钩子点）。要移植到 Claude Code / Codex 等，需要的接缝只有三类：

1. **工具注册**（MCP 是通用路径）
2. **工具执行后追加上下文** ← **这一条最关键**，锚就是靠它注入的
3. **能看见用户发言 / 回合边界**

`adapters/` 里留了位置与说明。**若你的宿主缺第 2 条，移植后它会退化成"只是个记事本"** —— 请如实告诉用户这一点。

## 6. 怎么验证装成功了

1. **重启后**，新开一个会话（在任意一个项目目录里）
2. 让 AI 调用 `plan_status`
3. **应该看到**：`【计划锚】当前没有生效的计划。` 后面跟一行 `（本项目作用域：<那个目录>）`

看到"作用域"那一行 = **它在工作，而且知道自己在哪个项目里**。
如果一直看不到任何 `plan_*` 工具 → 没加载成功，回到第 5 节第 3 步检查配置。

**推荐做一次真实验收**：给 AI 一个**多步任务**（≥3 步），看它会不会主动立计划；中途塞一个新问题，看它会不会显式处置而不是直接追下去。

## 7. 怎么卸载 / 回滚

```bash
# 1) 从 profile 移除
dsh plugin --profile <profile-name> remove dsh-plan-anchor

# 2) 重启宿主

# 3) 数据库留不留由用户决定（它只含计划与台账，不含代码）
#    删除：删掉 $DSH_HOME/plan-anchor/plan.db
```

**卸载是干净的**：它不在你的代码仓库里留任何东西（`plan.db` 在 `$DSH_HOME` 下，不在项目里）。

---

## 附：给 AI 的一页纪律

装好之后，**你自己要遵守的**（这些不是可选项，是这个工具能生效的前提）：

1. **多步任务（≥3 步）先立计划**，再动手
2. **中途发现新问题 → 先处置再动手**：`plan_discover` 选 permit / defer / decline。**不许默默切换。**
3. **选 defer 必须给回程条件**（"什么时候回来处理"），否则那条欠账会烂掉
4. **别自己宣布完成**：最后一步做完时，先问用户
5. **确实在做当前步的长活 → `plan_note` 声明在轨**，免得被误判成漂移
6. **要问用户时先过 `plan_ask`**：它会告诉你该不该问、以及"两边理由都要给"

完整的判据与不变量见 [`SPEC.md`](SPEC.md)。
