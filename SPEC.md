# SPEC —— 计划锚的规范

> **这是一份可移植的设计规范，不是某个宿主的实现说明。**
> 参考实现在 [`dsh/`](dsh/)，词表的完整推导在 [`research/drift-signals-zh.md`](research/drift-signals-zh.md)。
> 如果你在另一个宿主上实现它，**以本文件的判据与不变量为准**。

---

## 0. 立场（先读这个，否则后面的判据看不懂）

绝大多数"防跑偏"工具的做法是**禁止偏离**。这个项目的做法不同：

> ### **偏离可以，沉默不行。**

理由来自它的目标场景：AI 在做多步计划时冒出新问题，**去处理新问题本身常常是对的**（那可能就是阻塞）。真正的病不是"它去处理了别的事"，而是：

1. **它不再记得原计划**（③）
2. **没人知道它偏了**（不透明）
3. **偏出来的东西没有归属**（既没放弃、也没记下）

所以全部判据围绕这三件事，而不是围绕"要不要拦"。

### 四条设计原则（判据冲突时按这个顺序裁）

| # | 原则 | 含义 |
|---|---|---|
| P1 | **只对可观测事实硬拦** | 计数、空字段、哈希、"用户到底有没有说过话"。语义判断一律不硬拦，只提醒或交给用户 |
| P2 | **禁止的是沉默，不是偏离** | 每个拒绝都必须给出合法出路；不给出路的拒绝视为设计缺陷 |
| P3 | **用预算，不用禁令** | 超支不禁止，但**代价必须持续可见** |
| P4 | **护栏自己的错误必须可见** | 护栏自身出错要写台账并打宿主日志；**静默失败是最坏的失败**——它同时具备"没报错"和"没生效"，谁都查不出来 |

---

## 1. 不变量表（I1–I12）

照抄 liza `INVARIANTS.md` 的写法：**逐条标注"在哪一层强制"**。
理由逐字来自 liza ADR-0039：*"security constraints belong in code, not in prompt instructions"* ——
所以凡能用代码拦的，绝不止于提示词要求。

| ID | 不变量 | 防的是什么 | 强制层 |
|----|--------|-----------|--------|
| I1 | 计划内同时最多一个 active 步骤 | 多头并进、主线丢失 | `code` |
| I2 | 新发现必须显式判定 `permit`/`defer`/`decline`，无判定即拒；`permit` 认定失败也落回 `defer`（安全侧） | 一路追新问题 + 泊位只进不出 | `code` |
| I3 | 已生效计划不可静默覆盖，必须带 `reason`，旧版留痕 | 计划被悄悄改写掉 | `code` |
| I4 | 状态迁移的台账由代码自动追加，不接受 agent 手工写 | 事后编故事 | `code` |
| I5 | 泊位条目**永不自动消失**，只有显式关闭才关闭 | 欠账被遗忘 | `code` |
| I6 | 偏离结束后由代码自动回到挂起的计划步骤 | "忘了回来" | `code` |
| I7 | 完成步骤必须附 `evidence` | 假装推进 | `code` |
| I8 | 回合开始与漂移超阈值时，把锚放回上下文 | 计划不再被读取 | **`prompt_only`** |
| I9 | 本步开始以来零工作痕迹（零工具调用且零用户回合）不许声称完成 | 假装推进 | `code` |
| I10 | 偏离额度超支后，代价持续显示在锚上（不禁止，但可见） | 静默超支 | `code` |
| I11 | 同一关卡连续被拒满 `refuseCap` 次后**放行 + 上报**，且熔断在本次调用内终局 | 无限卡死 / 两关乒乓 | `code` |
| I12 | **完成闸门**：最后一步做完不许自称完成；必须先问过用户（自结束时以来**确有用户发言**这一可观测事实），再由 `plan_review` 记录用户裁定；用户说没完成则**如实退回** | 自己拍板交付 / 静默替用户下结论 | `code` |

### ⚠️ 三条必须照实告知使用者的注记

1. **I8 是唯一只能靠注入提醒实现的**，因此标注 `prompt_only` —— **不假装它是硬约束**。
2. **I2 与 I7 的"必填"故意不放在参数 schema 层。** schema 报错只会说"缺参数"，代码层拒绝才能把"为什么必须要"讲给模型听。
3. **I12 的判据是"可观测事实"而不是"感觉"**：它在用户自 AI 宣布完成以来**确实发过言**时才放行。这是 P1 的直接应用。

---

## 2. 引用一个步骤：序号 vs id

这一节是被**现场**加进来的：本项目的作者在**两轮之内、两次**把「第几步」当成了 `id` 填进参数里，
被工具拒绝。一个"按位置插入"的操作却要求一串不透明的主键 —— **填错两次这件事本身就是设计缺陷的证据。**

所以规范里定死三条：

### 2.1 两个东西，必须分清

| | 序号（`ord`） | 身份（`id`） |
|---|---|---|
| 是什么 | **给人看的显示号** | **稳定不变的身份** |
| 长什么样 | 第 1 步、第 2 步… | 28、29、35… |
| 会变吗 | **会** —— 插入/删除步骤后整体顺延 | **不会** —— 跨修订、跨重规划都跟着走 |
| 谁该看 | **人和模型**（读） | **机器**（精确引用） |
| 泊位同理 | 「泊位 1、2、3…」（连续） | 数据库 id（**不连续**，7、5、6 这种） |

**额外步骤那套序号（「额外步骤 1、2、3」）是第三套，跨修订连续，不要和上面两套混用。**

### 2.2 输入侧：**两套都收，id 优先，兜底要说明**

任何"引用某个步骤/泊位"的参数，都必须成对提供：

```
step_id: 28       // 精确（稳定）
step_ord: 1       // 人话（会变，但好填）
```

解析顺序**必须是**：

1. **先按 id 精确查** —— 查到就用，这是正常路径
2. **查不到，再按序号解释**（只有当**没有任何**步骤的 id 等于它时才这样做，所以不会误伤）
3. **无论走哪条，都要在回执里说清"按什么理解的"** —— 例如：
   > ℹ `step_id=1` 不是任何步骤的 id；我按**主线第 1 步**理解（它的 id 是 28）。

**绝不静默改写。** 这是 P2（禁止的是沉默）在编号上的直接应用。

### 2.3 输出侧：**给人看的只出现序号**

- 面向人的输出里**不出现 id**，也不出现任何内部代号
- 泊位一律显示「泊位 N」（**连续序号**），**不要**暴露不连续的数据库 id
- 需要精确引用时（例如给 AI 读的详细列表），才把 id 标出来，并写成 `第 1 步(id=28)` 这种**一眼能看出是两种东西**的形式

### 2.4 报错侧：**报错必须教会人**

解析失败时不能只报"找不到"。必须同时给出：

- **点明两者不是一回事**："要的是 id（形如 28），不是「第几步」的序号（形如 1）"
- **两条出路**：填 id，或者改用序数参数
- **当前对照表**：`第1步→28、第2步→29、…`

> 判据：**一个报错如果让人还得再问一次"那我该填什么"，它就是不合格的报错。**

---

## 3. 三值处置（I2 的展开）

任何新发现的问题，必须落到三值之一，**没有第四个选项**：

| 处置 | 含义 | 代价 |
|---|---|---|
| `permit` | 它**阻塞**当前步，现在正式中断去处理 | 计入偏离额度（偏离是有价的） |
| `defer` | 现在不做，**入泊位**并带回程条件 | 占一条欠账，到期会提醒 |
| `decline` | 判定不做，**并写明为什么不做什么** | 记入台账 |

**回程票（`defer` 的必要条件）**：必须写清"**什么时候回来处理它**"。
- 最优写法：绑定到**步骤身份**（"第 N 步做完之后"），这样中间插入/删除步骤时不会指错
- 次优：写明具体条件（"计划走完之后"）
- **只写"以后再说"不算回程票** —— 那样的欠账会烂掉

---

## 4. 问询判据（什么时候该问用户）

规则来自三份一手材料，不是拍的：

- **AAEF《审批质量与审批疲劳》**：*批准不构成授权，除非批准者有足够的上下文、能力、时间与独立性*；泛泛地问只是 "approval-shaped friction"（有审批的形，没有审批的实质）
- **Horvitz 期望值框架**：`E[打扰] = P(相关)×收益 − (1−P)×误扰成本` → **被问过多次之后，连相关的问题都该闭嘴**
- **Carey & Everitt《Human Control》**：因"对方不理智"而忽略指令是**被命名的失败模式**；且 agent 不得 *inappropriately influence* 用户下决定的能力

### 三档

| 档 | 什么时候 | 行为 |
|---|---|---|
| **`must`**（必问） | 不可逆动作 · 验收判据的最终确认 · 超出已批准范围的新工作 | **不许静默继续** |
| **`once`**（问一次） | 发现与原计划冲突的事实 · 要改计划结构 · 拿不准算不算偏离 | 问一次，**用户怎么说就怎么办** |
| **`note`**（不问，只记账） | 纯执行细节 · 用户明确授权过的类别 | 写台账，**收尾时一起摆出来** |

### 三条铁律

1. **不许泛泛地问。** 必须说清"要做什么具体动作、影响什么"。泛泛地问 = approval-shaped friction。
2. **必须两边都给**：照做的理由 · 不照做的理由（含代价与不可逆性）· 我的建议（标明是建议）。
   **只摆一边 = 不当影响。**
3. **绝不因为"用户可能不理智"而不听他的决定。** 只允许「提出一次 + 摊开代价 + 照办 + 记账」。

### 疲劳控制

同一天问够 `askBudget`（参考实现默认 5）次后，**非 `must` 档自动降级为 `note`**。
**`must` 档不受影响** —— 该问的永远问。

---

## 5. 信号词表（只留词与频次）

完整推导见 [`research/drift-signals-zh.md`](research/drift-signals-zh.md)。样本：260 个会话 / 4895 条用户原话 / 281 万字。
**检测的是"用户说的话"**（与"AI 的行为"互补：漂移预算看的是工具调用序列）。

### A. 打断 / 叫停类（最高优先：触发"回到锚 + 复述当前步"）
`等一下`(94) · `停一下`(10) · `先停`(10) · `先别`(30) · `先不`(48) · `打住` · `暂停`(24) · `慢着` · `别急`(14)

### B. 改优先级类（触发"确认是否要改计划顺序"）
`你先…`(216) · `先…吧` · `先做`(46，**仅当后面接的是新目标时**才算)

### C. 追加新需求类（触发"要不要入泊"）
`对了`(80) · `另外`(20) · `顺手`(24) · `再加`(34) · `加一个`(42) · `还要`(32) · `补充`(46)

### D. 改向 / 纠错类（触发"计划是否要修订 / 返工"）
`改成`(31) · `换成`(8) · `弄错`(6) · `不是这样`(4) · `重来`(10) · `重新做` / `重新来`

### E. **排除表**（明确不报，否则误报率会爆）
`反正`(54) · `一起`(60) · `帮我`(82) · `等等`（除非构成"等一下"）· `还有`(296，单用) ·
`重新`(186，单用) · `不对`(106，句中) · `先做`(46，表格 / 计划内语境)

### F. 判据约束（**比词表本身更重要**）

1. **成短语才算**：`等一下` 算，`等等` 不算；`先别` 算，`先` 单字不算
2. **位置加权**：句首 / 消息开头的信号价值远高于句中出现
3. **组合加分**：一句里同时出现"打断类 + 新目标" → 强信号
4. **不自动 park**：只触发**提问**，不代替用户决定
5. **软硬分开**：打断类 → 可自动汇报进度；追加 / 改向类 → 只问不做
6. **判"指令还是任务描述"看长度**：≤80 字才更可能是对 AI 的直接请求

### ⚠️ 中文特有的坑（英文方案直接照搬会踩）

- **短词会被惯用法吃掉**：`等等` 70 次是"诸如此类"、只有 8 次是"等一下"（**87% 假阳性**）；
  `先不` 会命中"先**不管** / 先**不说**"这类非叫停用法。
  → **绝不能用子串匹配单字或双字**，必须用完整短语 + 位置约束。
- **高频 ≠ 信号**：上表 E 里的词频次都不低，但没有一个是信号。
- **语音输入噪声**：真实输入常有语气词、无标点、同音误转。**不能依赖标点做句边界。**

---

## 6. 参考实现必须提供的接缝

要在新宿主上落地，只需要三类接缝：

| # | 接缝 | 用途 | 缺了会怎样 |
|---|---|---|---|
| 1 | **注册工具** | 让 AI 能调用 `plan_*`（MCP 是通用路径） | 完全不可用 |
| 2 | **工具执行后追加上下文** | 注入锚、回锚、漂移提醒 | **退化成"只是个记事本"** —— 能记，但没人看 |
| 3 | **能看见用户发言 / 回合边界** | 回合锚、回锚、完成闸门的"用户是否发过言" | I8 与 I12 失效 |

**第 2 条最关键。** 如果宿主没有它，请**如实告诉使用者**这个退化。

---

## 7. 已知边界（不是待办，是能力边界）

1. **只在 AI 主动申报时有效**：不调工具直接干活，护栏看不见
2. **不能阻止偏离，只能让偏离不被忘记、不被隐藏**
3. **防不住改数据库**：能改磁盘文件的程序，没法用同一磁盘上的东西约束它
4. **不判断"做得对不对"**：验收是语义判断，留给人
5. **熔断机制形似"重复施压即可通过"** —— 因此必须：次数有上限、每次拒绝留痕、放行时**强制在主视图上标出**

> 这五条请**照实转告使用者**。藏着它们，用户第一次踩到就会认为整个工具在骗人。

## 8. 等待：必须有**两条**出边

「等外部依赖」看起来像一张标签，其实是一个**状态**。成熟系统都是这么建的：

| 来源 | 做法 |
|---|---|
| **blocked.md**（给 AI agent 写的规范） | *blocked means implementation cannot proceed and **only a human can unblock it**. Stop, set status: blocked, and escalate.* —— 判据是「**重试也没用**」，这条把"等待"和"卡住了"分开 |
| **BPMN / Camunda** | 中间捕获事件（停下来等）+ **边界定时器事件**（挂在等待上的超时出口） |
| **Temporal**（人机回路教程） | **Signal**（唤醒）："Unblock waiting conditions — Resume a paused Workflow that's waiting for input" + **Durable Timer**（超时）：处理 *approval deadlines* |
| **agent 状态机实践** | *Pauses are just "stop and wait for the next API call"*；显式的 `AWAITING_ANSWERS` / `AWAITING_APPROVAL` 状态 |

### 规矩

**「等待」= 三要素 + 两条出边。**

1. **等谁 / 等什么**（`what`）
2. **什么条件算"等到了"**（`until`，即唤醒条件）
3. **超时之后干什么**（`on_timeout`）

两条出边：**①唤醒**（条件满足）· **②超时**（条件一直不满足）。

**三个要素与两条出边，缺一不可。** 少了「超时之后干什么」，等待就变成一张**无限期的免死金牌** ——
那等于给漂移开了一个正式的后门。

### 两条附则

- **等待不涨漂移预算**：等外部不是 agent 的错。**等自己才是漂移。**
- **等待只用于「重试也没用、只能等外部」的事**（blocked.md 的判据）。可以重试解决的，不是等待，是失败重试。

### 已知的正确对照

| | |
|---|---|
| ❌ 差的写法 | 「我在等外部依赖」（没头没尾，等于免死金牌） |
| ✅ 好的写法 | 「**在等**：你决定仓库标签填不填 · **等到**：你回话 · **超时**：3 天没动静就先把标签留着、继续别的事」 |

## 9. 计划演进度量（**照搬论文的公式**）

问题：**"这个计划长了多少"不该靠感觉。** 但只记一个步数是错的 ——
「4 步变 4 步」听起来没变，其实可能**整批步骤被换掉了**。

### 9.1 出处

**《From Plan to Action: How Well Do Agents Follow the Plan?》**（[arXiv:2604.12147](https://arxiv.org/abs/2604.12147)）
用三个维度量"agent 跟了多少计划"，并用**几何平均**汇总：

```
PC = (PPC · POC · PPF)^(1/3)
```

论文原话：*"Geometric mean aggregates sub-metrics multiplicatively, ensuring **equal weighting and
preventing compensation** across dimensions."* —— **一个维度烂，总分就得烂。**

### 9.2 本项目的搬法

论文量的是「**轨迹 vs 计划**」；本项目量的是「**最初计划 vs 现行计划**」——
把「Φ」（计划指定的阶段）换成**计划诞生时的步骤集合**，把轨迹换成**现行步骤集合**：

| 维度 | 公式 | 含义 |
|---|---|---|
| **PPC′ 覆盖** | `\|原 ∩ 现\| / \|原\|` | 最初的步骤**还有几个活着** |
| **POC′ 顺序** | `LIS(原步骤在现行计划中的位置) / \|原\|` | 相对顺序**有没有被打乱**（「LIS」= 最长递增子序列，只看首次出现位置 —— 与论文一致） |
| **PPF′ 保真** | `\|原\| / \|原 ∪ 现\|` | 现行计划里**有多少是原来就有的** |
| **PC′ 综合** | `(PPC′ · POC′ · PPF′)^(1/3)` | 几何平均 |
| **膨胀率** | `1 − PPF′` | 单看"有多少不是原来的" |

### 9.3 判据必须是**稳定身份**（这一条最关键）

三个公式全都建立在**"这个步骤是不是原来那个"**上 —— 所以判据必须是
**稳定的步骤 id**（见 §2），**不能是文字比对、更不能只记步数**。

对照（同一个"4 步"）：

| 情况 | 只记步数 | 用 id 做集合运算 |
|---|---|---|
| 原 4 步原样保留 | 没膨胀 ✅ | 膨胀 0% ✅ |
| 原 4 步**全被换掉**、换成 4 步新的 | **"没膨胀"** ❌ | **膨胀 50%** ✅ |

**"靠身份而不是靠数数"** —— 这正是 §2（序号 vs id）那条规矩在度量上的用处。

### 9.4 这是**度量**，不是判罪

论文把立场写得很清楚：*"Including additional actions beyond those in the recommended plan is
**not necessarily negative, but can be distracting**."*

所以本项目把它显示出来、不改行为。理由是论文的另一条实测：
*"inserting additional task-relevant phases in the early stage **can degrade performance**,
particularly when these phases do not align with the model's internal problem-solving strategy."*
—— **这个代价值得被看见，不值得被禁止。**

### 9.5 已知限制

- **老计划没有出生记录**：本度量需要一个"出生时的步骤 id 集合"。本字段是后加的，
  所以**在它之前创建的计划，演进度量永久缺失**（不是算错，是没有数据）。
- **不含额外步骤**：额外步骤走的是另一套编号（见 §2.1），单独计数，**不混进主线膨胀率**。

## 10. 多条线：一个目录可以同时有多条计划（政企场景）

### 10.1 为什么需要

原来一个目录**只认一条活跃计划**，于是：

| 场景 | 原来的结果 |
|---|---|
| 两个会话在同一目录各干各的 | **后立的把先立的静默作废** —— 先那个会话查不到活跃计划，**锚不再注入，护栏静默失明**（没报错、没提醒） |
| 同项目两条独立的线（加功能 / 还技术债） | 只能塞进一条，计划变胖（而论文实测膨胀有害） |

**政企项目里这两种是常态**，所以必须支持。

### 10.2 模型：一个工作台，多条线，按会话认领

```
一个目录（工作台）
 ├─ 计划A（会话1 认领）← 会话1 只看见这条
 ├─ 计划B（会话2 认领）← 会话2 只看见这条
 └─ 计划C（已被取代）   ← 历史，留着
```

`plans` 表新增 `owner` 列（哪个会话认领）。

### 10.3 `activePlan` 的三档（**这一条是核心**）

| 情况 | 返回 |
|---|---|
| **本会话有认领的计划** | 用它 |
| **本会话没有 + 这个目录恰好一条** | 用它（换会话/重启后无缝接上，不影响别人） |
| **本会话没有 + 有多条**（真的多会话并存） | **返回 null —— 不许猜** |

**"猜"就是互踩的来源**：你会不知情地操作别人的计划。

### 10.4 `plan_set` 撞上别人的计划时：**并存，不对抗**

| 情况 | 行为 |
|---|---|
| 撞上**自己的**计划 | 需要 `reason`；回执**显著说明**"你取代了『X』，它的进度不再算数" |
| 撞上**别人的**计划 | **不动它、也不拦** —— 新计划与它**并存**（各认各的） |
| 要**显式取代**别人的 | 带 `replace: true` + reason（会记 `plan_takeover` 台账：谁取代了谁） |

**不许静默作废** —— 这是本项目的核心原则在多会话场景下的落地。

### 10.5 还要配一条约束，否则会烂

**允许开多条线，就必须让"开了几条"看得见**，否则线太多没人收尾
（政企场景里，"线太多"往往比"线不够用"更常见）。

锚里应当能显示本目录的活跃线数。**这一条还没做**，先记在这里。

### 10.6 已知未做

- **钩子路径**（回合锚/提醒）不走 `withRefs`，那里的"当前会话"是空串 → `activePlan` 退回"目录唯一一条"。多会话并存时，锚可能显示到别人的计划。**这是已知缺口。**
- 本目录活跃线数的显示（见 10.5）。
