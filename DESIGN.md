> **这份文件是设计推演，不是使用说明。**
> 想装它请看 [`AI-INSTALL.md`](AI-INSTALL.md)；想看规范与不变量看 [`SPEC.md`](SPEC.md)。
> 这里保留的是**当初为什么这么设计**：每条判据的证据、踩过的坑、以及被明确否掉的替代方案。

---

# dsh-plan-anchor · 计划锚（防漂移护栏）

> 一句话：**执行计划时冒出来的新问题，一律先"泊"下来，不许当场追；而计划本身每回合自动放回你眼前。**

## 它解决什么

用户原话描述的问题：

> 已经按照一定的规划规划好了，然后再执行其中一个规划的时候又发现了很多其他的问题，
> 然后就一路沿着其他的问题进行下去了，导致其他的规划全忘记了，并且混乱了。

这不是"没记下来"的问题，而是**注意力调度**的问题：

| 环节 | 发生了什么 |
|---|---|
| ① | 计划活在**易失的对话上下文**里 |
| ② | 执行到某一步，冒出一个新问题——**具体的、当下的、有明确解法的** |
| ③ | 而计划里的后续步骤是**抽象的、遥远的**；注意力天然被前者劫持 |
| ④ | 追下去，每解决一个又冒新的（递归 yak shaving） |
| ⑤ | 原计划不是被删了，而是**再也没被读过** |
| ⑥ | 回来时不知道"我干到哪了、为什么在这" → 混乱 |

### 为什么"已经有任务清单"还不够

本机已装 `dsh-task-tracker`（Beads 式层级任务库：层级任务 + 依赖图 + ready 查询）。它治不了这个病，因为：

1. **它是被动的**——只有主动查才输出。而漂移的定义恰恰就是"没人去查它"。
2. **它没有 WIP 上限**——新任务可以直接 `in_progress`，与计划任务平权竞争，零摩擦。
3. **它不区分"计划内步骤"与"计划外发现"**——两者同一命名空间，地位平等。
4. **它没有位置指针**——它知道你有哪些任务，不告诉你"你刚离开了第 2 步、现在该回第 3 步"。
5. **它不介入时机**——它在工具调用之外，不在"诱惑发生的那一刻"。

本插件补的正是这五样。

## 四个抓手

| 抓手 | 针对上面哪一条 | 怎么做 |
|---|---|---|
| **持久锚** | ①⑤ | 计划/步骤落盘 SQLite（`$DSH_HOME/plan-anchor/plan.db`），跨会话、跨上下文压缩存活 |
| **泊位队列** | ②③④ | `plan_discover` 是新问题**唯一**的入口，必须显式判定 `permit/defer/decline`（不许含糊留在半空），默认落回 `defer` 并当场把焦点按回当前步 |
| **回合自动重放** | ⑤ | 每个用户回合的第一次工具调用后，自动注入一行【计划锚】；漂移预算耗尽时自动注入纠偏提醒 |
| **代码强制的不变量** | 全部 | 见下表——能用代码拦的绝不写成提示词要求 |

## 工具

| 工具 | 用途 |
|---|---|
| `plan_set` | 立计划（有序步骤）。每步可以是字符串，也可以是对象 `{text, acceptance, files, commands}`。已有生效计划时必须传 `reason`。**注意：它是"换计划"（整份重建），局部调整请用下面三个修订工具** |
| `plan_amend` | **原地修订**某一步的内容（文字/验收/scope）。编号不变、身份不变、历史保留 |
| `plan_insert` | 在某一步**之后插入**步骤。当前焦点不会被带跑；编号顺延并明确告知 |
| `plan_drop` | **丢弃**一步（只标记不删行）。若丢的是当前步，焦点自动顺延到下一个待办 |
| `plan_status` | **回归锚**：我在第几步 / 下一步 / 泊位几条 / 漂移预算用了几次 / 是否在漂 |
| `plan_step_done` | 完成当前步（`evidence` 必填）。处于偏离态时，完成后**自动**回到挂起的计划步 |
| `plan_discover` | 执行中发现新问题 → **必须显式判定三值处置**：`permit`（阻塞当前步，现在做）/ `defer`（入泊，稍后，**必须同时给 `resume_when` 回程票**）/ `decline`（判定不做，须附理由） |
| `plan_goto` | 显式改焦点：回到某步 / 把泊位条目提上来做。必须写 `reason`，计入台账 |
| `plan_park` | 泊位清单（永不自动消失，只能显式关闭） |
| `plan_close` | 显式关闭泊位条目（判定不做）。`reason` 必填——泊位只进不出会让欠账清单烂掉 |
| `plan_review` | **完成闸门**：最后一步做完后必须先 `ask_user_question` 问过用户，才能记录用户裁定。`confirmed=false` 会把最后一步**如实退回**未完成 |
| `plan_log` | 漂移台账（append-only，由代码自动写） |
| `plan_mute` | 静音提醒 N 次调用（防"哭狼来了"） |

## 多项目隔离：一个项目一份计划（按会话工作目录）

存储是**单个 SQLite 文件**（`$DSH_HOME/plan-anchor/plan.db`），但**计划按工作目录隔离**：

- `plans.scope` = 会话的工作目录（取法与 DSH 自己的文件工具一致：`exec.agent?.session.header.cwd`）。
- 所有运行态（当前第几步、漂移预算、偏离额度、熔断计数、静音）都挂在 **`plan_state(plan_id, key)`** 上，
  **隔离由 `plan_id` 天然保证**——不需要给每个键加前缀，也就不会漏。
- 生效计划是**查出来的**（`WHERE status='active' AND scope=?`），不是一行全局指针，
  所以**项目 A 与项目 B 各有各的 active 计划，互不覆盖**。
- Windows 下路径大小写不敏感、尾斜杠会被归一化，同一目录的不同写法归到同一作用域。
- **同一个项目的不同会话共享同一份计划**（计划属于项目，不属于窗口）——这正是"写到硬盘"的价值。
- 拿不到 cwd（非 agent 调用）时退化为默认作用域 `""`；老库里的计划也归属默认作用域，**迁移不丢数据**。

> 为什么不做"一个项目一个 .db 文件"（物理隔离）：风险从来不在"文件放一起"，而在"指针没分开"。
> 单文件换来三样实打实的好处——**台账可跨项目检索**、**备份只拷一个文件**、
> 不必决定"数据库放项目目录里会不会污染仓库"。

## 泊位的「回程票」：ready-to-resume

泊位只记"问题是什么"是不够的——**光记下问题不记回程，泊位就会变成没人再看的垃圾抽屉**。
所以 `defer` 必须同时给 `resume_when`（免费文本，如「第 4 步做完之后」），可选 `resume_after_ord`（主线步骤号）。

这不是记账负担，而是**本次设计里唯一有干预实验支持的机制**：
Leroy & Glomb (2018), *Organization Science* 29(3):380-397 摘要逐字——
> "A **ready-to-resume intervention**, in which one briefly reflects on and **plans one's return to the interrupted task**, **mitigates this effect**"

给了 `resume_after_ord` 之后，**回程不靠记忆，靠代码在该步做完时主动举手**：

```
⏰ 回程票到期：泊位 #4「日志格式不统一」—— 当初写的是「第 4 步做完之后」，现在到了（主线第 4 步已完成）。
```

配套的两条同类证据：Masicampo & Baumeister (2011) 表明"写下来"这个动作本身就能释放认知资源
（所以入泊是**卸载**，不是记账）；Rubinstein, Meyer & Evans (2001) 表明切换代价随 **task cuing** 下降
（所以锚行要指向"该做什么"，而不只是一个编号）。

## 步骤的两个可选字段：验收动作 与 scope 声明

步骤除了 `text`，还可以带两个字段。**都是可选的**——不带就退回原来的行为，不会有任何额外打扰。

```js
plan_set({
  title: "把电梯 demo 上 Docker",
  steps: [
    { text: "写 Dockerfile",
      acceptance: "docker build 通过且镜像 <200MB",      // ← 操作级 cue
      files: ["Dockerfile", "*.dockerignore"],           // ← 这一步允许动什么
      commands: ["docker build*"] },
    "写 compose 编排",                                    // 纯字符串照样可以用
    { text: "本地跑通", acceptance: "curl localhost:8000 返回 200" }
  ]
})
```

### `acceptance`：把"算做完了"前置

没有它，锚行只能给我一个**标签**（`主线第 3/6 步：写 compose 编排`），"做完了没有、标准是什么"全靠临场判断——
这正是"做了一半就说做完了"的温床（I7/I9 只能挡住"什么都没干就说完了"）。有了它：

```
▶ 当前：**主线第 3/6 步**：写 compose 编排（验收：docker compose up 后三个服务 healthy）
```

完成时也会回照验收：`主线第 3 步完成 ✔（对照验收：…）`。
依据：Rubinstein, Meyer & Evans (2001)——**切换代价随规则复杂度上升、随 task cuing（提示）下降**，
所以"提醒我该做什么、做到什么程度"比"提醒我干到第几步了"有效得多。

### `files` / `commands`：scope 判决（**当前是 observe 档，只记录不拦**）

给步骤声明"允许动什么"之后，**代码自动给每次工具调用判一次**：

| 判决 | 含义 |
|---|---|
| `match` | 在范围内 |
| `out-of-scope` | **会改动东西**的工具动了范围外的目标（写文件、跑命令） |
| `partial` | 部分越界 |
| `read-outside` | 越界**读**——单独记一类，**不算越界改**（否则误报率高得没法看） |

判决写进 append-only 的 `verdicts` 表，`plan_status` 里有一行仪表盘：

```
🔎 scope 判决（observe 档，仅记录不拦）：match 12 / out-of-scope 1 / read-outside 3 —— 其中 out-of-scope 1 条，需人工看一眼是不是误报
```

**为什么默认只观察、为什么没实现 block 档**（这是刻意的，不是没做完）：

1. 研究给出的直接教训是 planlock 的默认档就是"先只观察"，先把误报率测出来再收紧。
2. stop-that-shit 的架构原则：**只对可观测事实做硬决定**——文件路径、命令名是可观测的，但"这次读越界算不算问题"仍带语义，误报必须用数据说话。
3. 真要做硬拦，**唯一"连用户也绕不过"的层级是 `PreToolUse` 的 deny**
   （官方逐字：blocks the tool *even in `bypassPermissions` mode or with `--dangerously-skip-permissions`*），
   而不是 post 层——本插件目前只在 post 层观察，所以**从一开始就不给自己造一个假的硬保证**。

`scopeMode` 三档：`off`（完全不判）| `observe`（默认，只记录）| `advise`（本步首次越界时**提醒一次**，仍不拦）。
`block` 档**故意未实现**：配置里写它会在 `apply` 期直接抛错，而不是静默降级。

## 完成闸门：把「算不算交付」交给用户（I12）

**问题**：`evidence` 和 `reason` 都只校验**非空**、不判质量——"已完成"这种废话能过。为什么不去判质量？
因为那是**语义判断**，而本插件的底线是"硬拦只用于可观测事实"（判语义会踩进 #5312「筛选掉诚实」的坑）。

**真正该问的问题是：这件事凭什么由 AI 自己拍板？** Anthropic 官方公布 auto mode 分类器对真实越界动作的
**漏检率是 17%**，且承认「**We've tried prompt-engineering around this and haven't found a fix**」——
所以"算不算交付"既不该由做事的那一方定，也不该交给另一个模型，**该交给会为结果负责的人**。

**但"问用户"这件事仍然可以被代码守住**——因为**"用户有没有回过话"是可观测事实**：

1. 最后一步完成 → 计划进入 `awaiting_review`，返回里明确写：**计划还不能算完成**，现在就用
   `ask_user_question` 问用户"这个计划真的交付了吗"，并把该步的 `acceptance` 摆出来当依据。
2. 此时若直接调 `plan_review(confirmed=true)` → **被拒**，理由逐字：
   > 自你宣布最后一步完成以来，这个会话里**没有任何用户发言** —— 这是可观测事实，不是我的猜测。

   这个判据来自 `agent/pre-step` 钩子：它看得见用户发言，也记得下时刻戳。**不是靠自觉，是比对时间戳。**
3. 用户回话后再调：
   - `confirmed=true` → 计划正式完成，台账记 `用户确认`。
   - `confirmed=false, note="用户原话"` → **那一步被如实退回未完成**（状态改回 active、证据里追加用户的意见），
     焦点回到它，台账记 `用户判定未完成`。**不是嘴上改口，是状态真的退回去。**
4. `completionGate: false` 可关闭（默认开）。

配套：**拿不准自己偏没偏时，也用 `ask_user_question` 问用户**——漂移提醒的文本里现在直接写了这一条建议。
"该不该算完成""我有没有跑偏"这类问题，问人不丢人，猜才丢人。

## 计划演化：改的是内容，不是身份（为什么不只有 plan_set）

**好计划不是一次定出来的，是执行中改出来的。** 这是设计里最深的一处修正。

**原来的病**（实测）：立 5 步计划，做完第 1、2 步（进度 2/5，焦点在第 3 步），到第 3 步时想调整计划 →
用 `plan_set` 的结果是：

```
【计划锚】…（v2）｜主线 0/6 步     ← 进度归零
▶ 当前：主线第 1/6 步：1 调研      ← 焦点跳回第一步
```

四处断裂：**进度归零、焦点跳走、步骤身份断了（全新行、新 id）、额外步骤归属与编号重启**。
根因是把计划建模成"不可变的版本快照"——那是**换**计划的语义，不是**改**计划。

**现在的模型**：

- **步骤以 `id` 为身份**。修订只改内容，不新建行、不丢历史。
- `plan_amend` —— 原地改文字/验收/scope，**编号不变**。
- `plan_insert` —— 插入后重排为连续 1..N；因为 `current_step` 存的是 id，**焦点与完成状态不会被带跑**。
- `plan_drop` —— 只标记 `status='dropped'`（不删行），编号收拢，理由留档。
- **额外步骤编号按「谱系」连续**（`plans.lineage_id`）：改计划不会让"额外步骤 1"指向两件不同的事。
- **编号变化必须主动说清**：每次修订写台账、进"修订史"，并在锚行上提示
  `⚠ 计划刚修订过：在主线第 4 步之后插入了 1 步；编号顺延：「上线」第5→第6步`。
- 步骤文字里自带的旧编号（如 `5 上线` 被顺延到第 6 步）会被标注 `（文字里的「5」是旧编号）`——
  **不改你写的字**（"3 个服务都要健康"不是序号），但**不许悄悄错下去**。
- `plan_set` 仍然存在，但语义收窄为"换计划"，且回执会**列出旧计划哪些步骤没有被继承**，
  并指引"如果你只是想局部调整，应该用 amend/insert/drop"。

## 不变量表

照抄 liza `INVARIANTS.md` 的写法：**逐条标注"在哪一层强制"**。理由逐字来自 liza ADR-0039：
*"security constraints belong in code, not in prompt instructions"* —— 所以本插件凡能用代码拦的，绝不止于提示词。

| ID | 不变量 | 防的是什么 | 强制层 |
|----|--------|-----------|--------|
| I1 | 计划内同时最多一个 active 步骤 | 多头并进、主线丢失 | `code` |
| I2 | 新发现必须显式判定 permit/defer/decline，无判定即拒；permit 认定失败也落回 defer（安全侧） | 一路追新问题 + 泊位只进不出 | `code` |
| I3 | 已生效计划不可静默覆盖，必须带 `reason`，旧版留痕 | 计划被悄悄改写掉 | `code` |
| I4 | 状态迁移的台账由代码自动追加，不接受 agent 手工写 | 事后编故事 | `code` |
| I5 | 泊位条目永不自动消失，只有显式关闭才关闭 | 欠账被遗忘 | `code` |
| I6 | 偏离结束后由代码自动回到挂起的计划步骤 | "忘了回来" | `code` |
| I7 | 完成步骤必须附 `evidence` | 假装推进 | `code` |
| I8 | 回合开始与漂移超阈值时，把锚放回上下文 | 计划不再被读取 | `prompt_only` |
| I9 | 本步开始以来零工作痕迹（零工具调用且零用户回合）不许声称完成 | 假装推进 | `code` |
| I10 | 偏离额度超支后，代价持续显示在锚上（不禁止，但可见） | 静默超支 | `code` |
| I11 | 同一关卡连续被拒满 `refuseCap` 次后**放行 + 上报**，且熔断在本次调用内终局 | 无限卡死 / 两关乒乓 | `code` |
| I12 | **完成闸门**：最后一步做完不许自称完成；必须先问过用户（自宣布完成以来**确有用户发言**这一可观测事实），再由 `plan_review` 记录用户裁定；用户说没完成则**如实退回** | 自己拍板交付 / 静默替用户下结论 | `code` |

**I8 是唯一只能靠注入提醒实现的**，明确标注为 `prompt_only` —— 不假装它是硬约束。
（I2/I7 的"必填"故意**不**放在参数 schema 层：schema 报错只会说"缺参数"，代码层拒绝才能把
"为什么必须要"讲给模型听。）

## 配置

```yaml
- id: plan-anchor
  name: dsh-plan-anchor
  config:
    path: !!js dshHomePath('plan-anchor/plan.db')
    driftThreshold: 12      # 连续多少次工具调用无计划进展 → 轻提醒（每步一次）
    escalateAt: 24          # 再超多少次 → 二次升级提醒（每步一次）
    detourBudget: 3         # 偏离额度：每提取一条泊位 -1，每完成一个计划步骤 +1；超支只可见、不禁止
    refuseCap: 5            # 熔断：同一关卡同一步骤连续被拒满 5 次后放行并记为"强制放行"（见下）
    watchTools:             # 在计划步骤未完成时调用这些"开新活"工具 → 轻提醒（每步一次）
      - task_create
      - todo_write
      - subagent
      - workflow
    turnAnchor: true        # 每个用户回合的第一次工具调用后注入一行锚
```

非法配置在 `apply` 期**快速失败并抛错**，绝不静默回退到默认值（照官方 guard 的契约）。
`escalateAt` 必须严格大于 `driftThreshold`。

## 模型体验（插件注入的原文）

### 回合锚（每回合一次，一行）

```markdown
【计划锚】把电梯 demo 上 Docker｜主线 2/6 步｜额外步骤 1/2 完成｜主线第 3 步：写 compose 编排｜泊位 2 条未处理
新发现的问题请先 plan_discover 显式判定处置（permit/defer/decline），不要直接开工。
```

### 两套编号（主线 k/n 与额外步骤 j）

**主线步骤 1..n 与额外步骤 1..m 是两套独立编号**——主线是对用户的承诺路径，额外步骤是从主线岔出去的工作。
混成一个序号，"我们做到哪了"就永远说不清，而这正是漂移里最恼人的那种混乱。

```
【计划锚】把电梯 demo 上 Docker（v1）｜主线 2/6 步｜额外步骤 1/2 完成
⚙ 当前在做额外步骤 2：修依赖版本冲突
主线第 3 步「写 compose 编排」已挂起 —— 做完 plan_step_done 会自动回到主线第 3 步。
🅿 泊位 1 条未处理（一律先入泊，不要现在追）：#4 日志格式不统一
```

完成额外步骤时的汇报也是两层：

```
【计划锚】… ｜主线 2/6 步｜额外步骤 2/2 完成
额外步骤 2 已完成 ✔（修依赖版本冲突）；**已自动回到主线第 3 步**：「写 compose 编排」
```

`plan_status` 会把两列分开列（主线带 `id=`，额外步骤独立编号 + `⊘中止`/`✔`/`⚙在做` 标记），
台账每行也标明是 `主线第N步` 还是 `额外步骤N`。**不允许出现把两者混在一起的序号。**

### 漂移轻提醒（每步一次）

```markdown
【计划锚 · 提醒】你已连续 12 次工具调用没有推进计划第 3/6 步「写 compose 编排」。
先停一下，明确回答：你手上正在做的事，属于上面这条吗？
- 是新发现的问题 → plan_discover 显式判定：permit（阻塞当前步，现在做）/ defer（入泊，稍后）/ decline（判定不做）
- 它卡住了当前步 → disposition="permit" 走正式中断
- 原计划已不成立 → plan_set 带 reason 显式重规划
泊位现有 2 条未处理。
```

### 开新活提醒（每步一次）

```markdown
【计划锚】你正在做计划第 3 步「写 compose 编排」，却调用了 `task_create` 开了一项新活。
如果这来自执行中冒出的新问题：先 plan_discover 入泊，再决定要不要现在做。
如果它确实属于当前步骤：忽略本条，继续。
```

### Token 与 KV Cache

- 回合锚：一行（约 40–60 字），每用户回合一次。
- 漂移提醒：达到阈值才出现，同一档同一步只响一次；60 次重复调用内最多两条。
- `plan_mute` 可要求静音 N 次调用。
- 只追加：注入内容位于可复用请求前缀之后，**不使已有 KV Cache 条目失效**。

## 实现要点

- **注入通道**：`ctx.on("tools/post-execute", …)` 返回 `additionalContexts`，把注意作为
  带来源标注的合成 user 消息（`source: {kind:'plugin', plugin:'plan-anchor', form:'notice'}`）追加在
  工具结果之后。没有来源标注的消息会被当成用户发言——这个标注是承重的。
  写法与官方 guard 包 `@deepseek-ai/dsh-repeat-tool-reminder` 一致。
- **回合边界**：`ctx.on("agent/pre-step", …)` 只负责打"本回合还没注入过锚"的标记，
  **不在 pre-step 里注入**（pre-step 的返回形态未核实，不猜）。
- **Fail-safe**：护栏自身的异常一律被 try/catch 吞掉——`observe()` 抛错绝不允许打断工具调用或主循环。
- **存储**：`node:sqlite`（与 `dsh-task-tracker`、`dsh-skill-evolver` 同款），WAL 模式。

## 设计依据（外部证据，非自说自话）

本插件的关键取舍不是拍脑袋来的，来自两份调研：
`docs/DESIGN.md` 里的调研记录（2026-09-18，57 条一手链接）与
一份本机调研笔记（2026-09-15，未随仓库发布）。
下列引用由调研逐字核实（本 README 只转述，未二次验证原文）：

| 结论 | 出处 | 它改变了本插件的什么 |
|---|---|---|
| 「任务清单是模型自己写的日记，不是监控摄像头。日记可以改，摄像头不能。」——清单由**执行期**的模型写、条目**没有 scope 字段**、状态**自报** | [planlock](https://github.com/) 的 ARCHITECTURE.md | 这就是"有 `dsh-task-tracker` 照样漂移"的**数据模型层**原因：它的字段只有 title/detail/status/priority/parent_id，结构上就无法承担漂移检测 → 本插件另起一套带位置指针与来源的结构，而不是复用任务库 |
| 详细**外部**清单显著优于通用自查：**10/10 vs 5/10（p=0.0325）** | [arXiv:2607.17937](https://arxiv.org/abs/2607.17937) | 计划锚做成**外部、代码持有**的结构，不依赖模型自查 |
| 任何"没回写就不许结束"的强制机制都必须自带：最大迭代上限 + 循环守卫 + 每轮可判定的收敛标准（厂商为此专门做了 `stop_hook_active`，并在 8 次连续阻断后强制结束回合） | Claude Code 官方 hooks 文档 | I8 只做**建议性注入**，绝不阻断回合；每步每档只响一次、有 `plan_mute` 静默阀——不制造无界循环 |
| 计划模式（plan mode）的阻断**不是绝对的**：开 bypass 权限时不生效；上下文压缩可能把声明边界的那条消息压掉——「要硬保证，就上 deny 规则」 | Claude Code 官方文档 | I1–I7/I9/I10 一律下沉到**代码**，不写成提示词要求 |
| 「调整必须**尽快**做出以减小进一步偏离」+「移除障碍是 Scrum Master 的职责」 | [Scrum Guide 2020](https://scrumguides.org/) | **修正了本插件最初的方向**：从"不许追新问题"改成"偏离有预算、只要不静默"——许可 `blocking=true` 与 `plan_goto`，只是让代价可见（I10） |
| 无引导时 agent 会 **chasing dead-ends** | Anthropic 自述 | 同上：正解是**预算**，不是**禁令** |
| `/goal` = 会话级 prompt-based Stop hook，且**由另一个新模型当评估者**判完成（`Not yet met` / `Met` / `Impossible`） | [code.claude.com/docs/en/goal](https://code.claude.com/docs/en/goal) | 给出了 I9 的形状：完成判据尽量**不依赖做事的那一方**。本插件用纯代码判据（零工作痕迹）而不是再加一个模型裁判 |
| `goal drift` 与 `asymmetric drift` 已被量化：所有被测模型都会漂，且与**累积上下文**正相关 | [arXiv:2505.02709](https://arxiv.org/abs/2505.02709) · [arXiv:2603.03456](https://arxiv.org/abs/2603.03456) | 说明"漂移"不是个人毛病，机制层面必须外挂 |

**反面证据（诚实记录）**：Claude Code 已**默认关闭** `TodoWrite`，官方理由是"较新的模型不写清单也能跟踪多步工作，
而工具定义与提醒本身占上下文"。→ 说明**"给每个任务都上清单"是错的**；本插件的对策是
**不立计划就完全不打扰**（单步任务零噪声），只在真正多步且会跨回合时才介入。

## 硬拦的边界：能硬拦的只有可观测事实

这是本插件最重要的一条设计原则，来自一个反面案例。

**反例**：`anthropics/claude-plugins-official#5312` 逐字——
> "An agent willing to falsely claim completion would have escaped in one turn. **The failure mode selects against honesty.**"

点的是「Stop hook 式硬卡」的失效模式：愿意**谎报**完成的 agent 一轮就逃脱了，而**诚实**报告"我卡住了"的 agent 会被反复拦住。**这个机制专门筛选掉诚实的 agent。**

**反向张力**同样存在：`#84002` 逐字——"I cannot get Claude to finish a task, I cannot get it to stay on one… **it routes around the control**"。太软的机制会被直接绕过。

所以正解不是"强制 vs 不强制"，而是**分层折中**（这也是 stop-that-shit「只对可观测事实做硬决定」与 planlock「只硬拦 `out-of-scope` 一类」的共同选择）：

| 层 | 本插件的做法 |
|---|---|
| **硬拦（代码）** | 只用于**可观测事实**：状态计数、字段是否为空、**本插件自己记录**的工具调用数。绝不判断"这件事算不算计划外""你现在是不是在漂" |
| **提醒（注入）** | 一切**语义判断**：漂移预算、偏离额度超支、开新活——只提醒、只让代价可见，不阻断 |

**并且本插件明确不做 Stop hook**：I8 只是往上下文追加一条消息，**从不阻断回合结束**。因此 `#5312` 的失效模式在结构上不会出现——拒绝的是一次**状态迁移**，不是"不许收工"；被拒的 agent 永远有合法出路（补字段、换路径、或干脆不调那个工具）。

### 为什么在 DSH 上「不做 Stop hook」不是偏好，而是唯一安全的选择

DSH 自带 Claude Code hooks 兼容桥 `@deepseek-ai/dsh-hooks-claude-code`，但**有两个致命缺口**（本节为主作者一手读源码核实，2026-09-18，非转述）：

| 缺口 | 一手证据 | 后果 |
|---|---|---|
| `stop_hook_active` **硬编码为 false** | `dsh/lib/index.js` L385–390：`stopPayload()` 返回 `stop_hook_active: false`；L394–395 注释逐字：*"`stop_hook_active` is present on SubagentStop only (**the loop-guard flag, always false**)"* | 官方文档教的"读到它为真就放行"这个**自限写法在 DSH 上永远失效** → 照抄范例 = 无条件继续 |
| **没有连续阻断上限** | 全文件 grep `consecutive` / `blockCap` / `BLOCK_CAP` **零命中**；`agent/turn-stopping`（L293）只做 `if (decision === "deny") agent.steer(...)`（L301） | Claude Code 那条"**连续 8 次阻断后强制结束回合**"的兜底在 DSH 上**不存在**，判据写错 = **真死循环、无兜底** |

补充：该桥**未挂载**在 desktop profile 的 `dsh.profile.bundles` 里；且只运行 shell 形态的 handler，`prompt`/`http`/`mcp_tool` 形态会被跳过——而 Claude Code 的 `/goal` 恰好依赖 `type:"prompt"`，所以那个「独立评估者」范式**不能直接照搬**（机制要自己实现）。

**结论**：任何「计划未回写就不许结束」的 Stop 闸门，若要在 DSH 上做，必须**自己**维护连续阻断计数、写死 cap、并在 cap 达到时**改为放行 + 上报**（绝不能继续拦），且**不得**依赖 `stop_hook_active`。本插件选择根本不走这条路：`additionalContexts`（注入提醒）比 `decision:"block"` 更安全，也正是官方推荐给"提醒回写计划"这类用途的语义。

**谎报是否比诚实更划算？（这是 #5312 的原始测试）** 逐项过一遍：诚实地说"这步不需要工具，因为讨论已定"（传 `no_work_reason`）与谎报"我做了工作"，**两者都放行**——诚实没有受到更差的待遇，因此不存在"筛选掉诚实"的选择效应。I7 只校验 `evidence` 非空，**不判定其真实性**（判真实性需要语义判断，超出硬拦的边界）。

**术语警告**（防止以后引用时踩雷）：调研对 arXiv 全库逐项证伪，以下名字在 LLM agent 文献里**并不存在**，不要引用：
`plan adherence rate`、`plan adherence score`、`subgoal completion rate`、`goal drift rate`、`task derailment`、`trajectory deviation`、`persona drift`。
`GDI (Goal Drift Index)` 属 SAHOO（arXiv:2603.06333），**不是** Arike 的指标；Arike 用的是 `GD_actions` / `GD_inaction`。
可逐字引用的名字：`goal drift`、`goal adherence`、`GD_actions` / `GD_inaction`、`instruction stability` / `instruction drift`、`PIF` / `PIF-N-K`、`50%-task-completion time horizon`、`Perturbation Recovery Rate (PRR)`、`Trajectory Efficiency`、`Narrative Commitment Preservation (NCP)`、`Fatigue Index (FI)`、`instruction-adherence drift`。

**注入位置有实验支持**：把全部指令追加到上下文**末尾**，`PIF` 平均回升 **22.3** 分（arXiv:2409.18216）——**位置比内容更重要，越靠近生成长点越好**。
本插件的注入走 `tools/post-execute` 的 `additionalContexts`，由循环追加在该步工具结果之后，方向正确；若将来要调优，应往"更靠近本轮末尾"调，而不是"更早、更频繁"。

### 熔断：强制力是"有限纠偏预算"，不是"绝对约束"

行业四家厂商的实测实现**无一例外**都设了硬上限——这不是本插件的发明，是生态共识：

| 机制 | 逐字规则 |
|---|---|
| Claude Code Stop hook | 「overrides the hook and ends the turn after **8 consecutive blocks**」（`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` 默认 8） |
| Claude Code auto mode 分类器 | 「**3 times in a row or 20 times total**」即暂停交还人类 |
| Cursor `stop` hook | `loop_limit` 默认 **5** |
| GitHub Copilot `agentStop` | **8** 次跑飞保护 |
| planning-with-files（26973★） | `PWF_GATE_CAP` 默认 **20** |
| oh-my-agent（1310★） | cap = **5** |

本插件的对应实现是 **I11**：同一关卡同一步骤**连续被拒满 `refuseCap`（默认 5）次**后放行，
并在台账写 `熔断放行`；**每一次拒绝也单独进台账**（`关卡拒绝`），所以"靠磨到熔断过关"的痕迹藏不住。

**实现上踩过的一个坑（值得记下）**：最初两个关卡（I7 evidence / I9 工作痕迹）共用同一个计数器，
结果第一个关卡熔断放行后，第二个关卡立刻又拒 → 计数器被重置 → **无限乒乓**。
这正是 cap 本来要防的死循环，等于自己造了一个。修法是：**熔断在本次调用内是终局的**——
一个关卡放行后，本次调用内后续关卡一律不再拦。
这个 bug 是**被单元测试抓出来的**（探针打印 `runtime` 表才看清计数器在 1↔2 之间震荡），不是靠读代码看出来的。

### 若将来要做"真硬拦"，对齐 PreToolUse/deny 而不是 Stop

一手证据：`PreToolUse` 返回 `permissionDecision: "deny"` 时，官方逐字说它
「blocks the tool **even in `bypassPermissions` mode or with `--dangerously-skip-permissions`**」——
这是生态里**唯一有官方硬保证**的阻断点（对比：交互式终端有 bypass 权限时 plan mode **不**强制）。
对应到 DSH 就是 `PreToolUse` → block 这一层。规划中的 scope 判决若要升级成硬拦，应对齐这一层。

**预期管理（官方量化天花板）**：Anthropic 工程博客公开 auto mode 分类器实测——真实 overeager 动作的
**假阴性率 17%**，原话逐字「**The 17% false-negative rate on real overeager actions is the honest number.**」，
并承认「**We've tried prompt-engineering around this and haven't found a fix**」。
→ **用模型当闸门，约 1/6 的真实越界会漏过，且提示词工程解决不了。**
所以本插件坚持用**纯代码判据**（计数、字段、状态），而不是再加一个模型裁判；
将来做 scope 判决也要配**结构性手段**（白名单、工具裁剪）兜底，不能指望判断力。

### 两条必须承认的设计边界（不是"待修"，是**能力边界**）

**边界一：护栏只在 agent「主动申报」时生效。**
它看得见的是**工具调用**（谁调了 plan_*、调了什么参数、本步有几调用）；它看不见的是
**"根本没申报的那件事"**。极端情况下 agent 完全可以不调任何 plan_* 工具、直接去改代码 —— 那时护栏一声不响。
红队实测：40 次计划外调用只发出 4 声提醒（回合锚 / 开新活 / 轻提醒 / 重提醒），之后全静默，且**台账里漂移零记录**。

→ 这一条**无法在插件层解决**。它是为什么"回合锚"（把计划顶到眼前）是承重墙、为什么
`plan_status` 必须能随时被用户问起的原因：**护栏不是警察，是记事本 + 闹钟；真正的裁判是用户。**

**边界二：绕开工具直接改数据库，只能做到「窄覆盖检测」，不是防线。**
`integrityLine` 能抓住"台账里入泊过、泊位表里却没有"这一类篡改（因为台账 append-only）。
但 agent 有全盘写权限，它可以：先删台账再删数据（无痕）／改 `plans.status` 让护栏彻底失明／
往 `plan_state` 写一个超大的 `mute` 让提醒永不触发（工具接口上限 200，写库无上限）。

→ 这一条也是**能力边界**：一个能改你数据库的程序，你没法用数据库里的东西去约束它。
**真正的隔离只有一条路：不信任边界内的自我约束，把该硬的规则放到 agent 够不到的地方**
（官方口径：`PreToolUse` 的 deny 是唯一连 `bypassPermissions` 也绕不过的层级 —— 而本插件目前
只在 post 层观察，**从一开始就不给自己造一个假的硬保证**）。

## 已知限制

- **I8 是建议性的，不是强制的**：提醒只是注入一条消息，模型仍可选择无视。真正硬的是 I1–I7。
- **漂移判定是启发式的**："连续 N 次调用无计划进展"不等于一定在漂移——长时间只读调研也可能合法。
  阈值可调，或 `plan_mute` 静音。
- **单一 active 计划**：故意不支持并行计划（并行正是漂移的温床）。要多线就开多个会话。
- **跳过 `plan_set` 就等于没护栏**：不立计划时插件完全不打扰（单步任务不该被啰嗦），
  但也就没有任何保护。配套技能 `plan-anchor` 负责"什么时候该立计划"这一触发纪律。
- **`turnAnchor` 每回合一行**：超长会话里累计可观，可关。

### 极端场景仿真抓出来的真实漏洞（2026-09-18，脚本驱动真实代码跑出来的）

本节由 `tests/_sim_plan_anchor_extreme.mjs` 的 5 个极端场景发现，**如实记录、不粉饰**：

| # | 漏洞 | 性质 | 处置 |
|---|---|---|---|
| 1 | **「不调工具、直接干活」是最大盲区** —— 没申报的事护栏完全看不见 | 结构性的，无法在插件层解决 | 只能靠**用户随时问一句"我们做到哪了"**；这是为什么"回合锚"要把计划顶到眼前 |
| 2 | **敷衍的 `evidence`（"已完成"）能过** | 语义判断，超出硬拦的合法边界（I7 只校验非空） | 不修：判质量必须靠语义，而"只对可观测事实硬拦"是本插件的底线。缓解=步骤带 `acceptance`，让**用户**能对着标准追问 |
| 3 | **敷衍的 `reason`（"因为我想换"）能过** | 同上（I3 只校验非空） | 不修，同上 |
| 4 | **磨关能磨过去**：连续被拒满 `refuseCap`(5) 次后放行 | 行业共识（强制力=有限纠偏预算），**刻意如此** | 已缓解：每次拒绝都进台账，磨关痕迹永久可见；过一步要烧 5 次调用 |
| 5 | 绕开工具**直接改数据库**删泊位 | 曾是无痕 | **已修**：`integrityLine` 全库自检——台账 append-only 且"入泊过的 id 在泊位表里必有对应行"，删掉即报 `⚠ 一致性异常` |
| 6 | **重规划会把旧计划的未闭合欠账弄丢**（泊位按 plan_id 归属，新计划看不见旧的） | 真 bug，违反 I5 | **已修**：重规划时把未闭合欠账搬到新计划（`resume_after_ord` 重置），并在回执里明说 |
| 7 | `plan_set` 的 `rules` 从未送达模型（render 只输出 briefing） | 真 bug：立计划时讲的规则等于没讲 | **已修** |

> 第 2、3 条不修是**刻意的选择**，不是漏做：判 `evidence`/`reason` 的质量需要语义判断，
> 而本插件的底线是"硬拦只用于可观测事实"。这两处的兜底是**人**——步骤带 `acceptance` 之后，
> 用户可以对着验收标准问"这条真过了吗"。

### 5 个极端场景的仿真结论（可复跑）

```
node tests/_sim_plan_anchor_extreme.mjs
```

| 场景 | 结果 |
|---|---|
| ① 新问题洪水（8 个发现全想"现在做"） | **结构上无法嵌套偏离**：permit 成功 1 次、被拒 7 次；其余只能入泊（并各带回程票） |
| ② 对抗性磨关（不写证据硬推） | 被拒 4 次 → 第 5 次熔断放行；台账留「关卡拒绝」×4 +「熔断放行」 |
| ③ 装死（零动作声称完成） | I9 挡住零动作；喂一次无意义工具调用后能过（= 漏洞 #2） |
| ④ 绕过尝试 | 无 reason 覆盖被拒；敷衍 reason 能过（=漏洞 #3）；直接改库被一致性自检抓到 |
| ⑤ 极端交错 + 重规划 + 堆 10 条泊位 | 两项目零污染 ✓；重规划后欠账仍在 ✓；收尾时把 11 条欠账举到眼前 ✓ |

## 冒烟测试

`tests/_test_plan_anchor.mjs` —— **136 项断言**，覆盖 I1–I12 与三类注入的触发时机/不重复性，
外加 I5 闭环（提取泊位→做完=关闭；提取→中止=退回待处理）、I9 零工作痕迹判据、
偏离额度的可见化、熔断（拒绝留痕 → 上限放行 → 不乒乓）、**两套编号必须分别报清**、
**多项目隔离**（两个工作目录各有各的计划、互不覆盖、默认作用域不被污染）、
**defer 必须给回程票 + 到期主动提醒**、**验收动作进入锚行与步骤表**、
**scope 判决**（match / out-of-scope / read-outside 分别记对，observe 档不打扰，判决也按项目隔离）。
运行：`node tests/_test_plan_anchor.mjs`

真实 harness 端到端也已验证（`dsh --profile plananchor "…"`）：老库**自动迁移**（`runtime` 指针迁到
`plan_state` 并按 plan_id 归属）、作用域隔离生效、新格式锚正常。
