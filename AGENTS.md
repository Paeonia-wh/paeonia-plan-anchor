# AGENTS.md —— 计划锚的纪律层

> 这份文件是给 **AI agent** 看的。它不依赖任何特定宿主：`rules` / `skills` / `AGENTS.md` / `CLAUDE.md`
> 这类机制都可以直接采用下面这份内容。判据与不变量见 [`SPEC.md`](SPEC.md)。

## 触发条件

**任务是多步的（≥3 步）、会跨多轮对话、或者用户给了你一份步骤清单** → 先立计划，再动手。

单发问答、一次性的小操作 → 不要立计划。（这个工具的原则之一是：**单发操作零噪声**。）

## 每个回合

- 进场时**先读计划**（`plan_status`），别凭记忆答"我们做到哪了"
- 发现任何**新问题** → **先处置再动手**，三选一：

| 处置 | 什么时候用 | 要求 |
|---|---|---|
| `permit` | 它**阻塞**当前步，现在就要处理 | 会记一次偏离（偏离是有价的） |
| `defer` | 现在不做 | **必须给回程条件**（"什么时候回来处理"） |
| `decline` | 判定不做 | **必须写明为什么不做什么** |

> **不许默默切换。** 这是这套东西唯一真正禁止的事。

## 要问用户之前

先过 `plan_ask` —— 它会告诉你该不该问（三档：必问 / 问一次 / 只记账），并要求你**把两边的理由都给**。

**三条铁律**（见 SPEC 第 4 节）：不许泛泛地问 · 只摆一边就是不当影响 · **绝不因为"用户可能不理智"而不听他的决定**。

## 在等外部时

如果这一步**卡在外部**（等用户回话、等 API、等别人交付）—— **不要含糊地说"我在推进"**，
用 `plan_wait` 显式声明，而且**三要素缺一不可**：等什么 / 什么算等到了 / **超时之后干什么**。

> 少了「超时之后干什么」，等待就变成无限期免死金牌。等待有**两条**出边：唤醒与超时。

等待期间不涨漂移预算（等外部不是你的错，**等自己才是漂移**）。条件到了就 `plan_note` 一句解除等待。

## 在做长活时

如果你确实还在当前这一步上、只是活很长（连续很多次工具调用）→ 用 `plan_note` 声明一句进展，
预算会清零。**它是"我在轨"的唯一表达通道**——不然合法的长活会被误判成漂移。

## 要改计划时

| 情况 | 用什么 | 不要用 |
|---|---|---|
| 改一步的内容 | `plan_amend`（原地改，编号与历史不变） | ❌ `plan_set` |
| 插一步 | `plan_insert`（编号顺延，会明确告诉你谁从第几变第几） | ❌ `plan_set` |
| 丢一步 | `plan_drop`（已完成的不许丢） | ❌ `plan_set` |
| 返工一个旧步骤 | `plan_rework`（记为支线，不计偏离额度） | ❌ 删了重来 |
| **整体换一份计划** | `plan_set` + `reason`，并用 `carry` 声明旧步骤的去向 | — |

## 收尾时

**别自己宣布完成。** 最后一步做完 → 先问用户（完成闸门），再用 `plan_review` 记录他的裁定。
用户说没完成 → **如实退回**，把他的话附在依据里。

## 用户说话时

宿主会检测几类用户信号并**直接把真实进度摆到你眼前**（打断类 / 问进度 / 要整理 / 追加需求）。
看到这类注入时：**照实答，不要凭记忆**；"要整理"那类要给一份他能直接看的稿子（零编号、零内部代号）。
